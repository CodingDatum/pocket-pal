import React from 'react';

// Here we have the option of adding an object as the parameter to this context provider
// When we add the different functions and variables this info will provide, it will autopopulate when being called in different components


const FlashcardsContext = React.createContext();

export default FlashcardsContext;