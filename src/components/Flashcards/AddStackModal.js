import React from 'react';
import Backdrop from '../UI/Backdrop';

const AddStackModal = props => {
    return(
        <Backdrop>
            <div>ADD STACK MODAL</div>
        </Backdrop>
    )
}

export default AddStackModal